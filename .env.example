# Ejemplo de variables de entorno para Vite
# Copia este archivo a `.env` y ajusta los valores según tu entorno.

# URL base para la API REST (incluye /api si tu backend la expone así)
VITE_API_BASE_URL=http://localhost:4000/api

# URL del servidor de sockets (socket.io)
VITE_SOCKET_URL=http://localhost:4000

# Título de la aplicación (opcional)
VITE_APP_TITLE=ProyectoLogisticaEventos

# Bandera para habilitar logs o características de desarrollo
VITE_ENABLE_DEBUG=true

# Ejemplo de clave de terceros (no la pongas real en repositorios públicos)
# VITE_MAPS_API_KEY=your_google_maps_api_key_here
